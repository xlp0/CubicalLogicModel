# Authentik Client Credentials
PUBLIC_AUTHENTIK_CLIENT_ID=your_client_id_here
AUTHENTIK_CLIENT_SECRET=your_client_secret_here

# Authentik URLs
PUBLIC_AUTHENTIK_BASE_URL=https://auth.pkc.pub
PUBLIC_AUTHENTIK_APPLICATION_SLUG=gasing-pkc

# Optional: Override default redirect URI (defaults to http://localhost:4321/api/auth/callback/authentik in development)
# PUBLIC_AUTHENTIK_REDIRECT_URI=http://your-domain.com/api/auth/callback/authentik
PUBLIC_AUTHENTIK_REDIRECT_URI=http://localhost:4321/api/auth/callback/authentik
