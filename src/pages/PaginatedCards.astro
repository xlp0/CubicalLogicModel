---
import FloatingNav from '../components/FloatingNav';
import PaginatedCardList from '../components/CardContent/PaginatedCardList';
import selectedCards from '../data/SelectedCards.json';

// Add debug logging
console.log('PaginatedCards page loading with cards:', selectedCards.cards);
---

<div class="h-screen w-screen bg-gray-900 text-gray-100">
  <FloatingNav client:load />
  <div class="container mx-auto p-8">
    <h1 class="text-3xl font-bold mb-6">Paginated Card Gallery</h1>
    <PaginatedCardList
      client:only="react"
      title="Model Cards"
      cards={selectedCards.cards}
      columns={5}
      gap={2}
      itemsPerPage={15}
    />
  </div>
</div>

<style>
  :global(:root) {
    color-scheme: dark;
  }
</style>
